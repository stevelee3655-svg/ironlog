# IronLog 인수인계 — 2026-08-27 (2차 갱신)

새 세션이 이 파일 하나만 읽고 이어갈 수 있게 쓴 문서다.
이전 판(2026-08-26 작성)은 낡아서 이 내용으로 갈아 끼웠다.

---

## 0. 30초 요약

개인용 아이폰 헬스장 운동 기록 PWA. **지금 상태는 안정적이고 급한 일은 없다.**

- 작업 트리 `C:\Users\이수환\.gemini\antigravity\scratch\ironlog-workout`
- 저장소 `stevelee3655-svg/ironlog` (**공개** — 수환이 명시적으로 선택)
- **푸시하면 자동 배포된다** → https://ironlog-3.vercel.app
- 마지막 커밋 `471b919` · 테스트 69개 통과 · 빌드·타입체크 통과
- 개발 서버는 `npm run dev` (포트는 비어 있는 곳을 알아서 잡는다)

---

## 1. 배포 — 2026-08-27에 방식이 바뀌었다

**옛 방식(폐기): 빌드 폴더를 압축해 호스팅에 복붙.**
수환이 "수정사항이 생길 때마다 새로 압축하고 복붙하는 건 너무 비효율적"이라며 바꿨다.

**지금 방식: `git push origin main` → Vercel이 자동으로 빌드·배포.**
Vercel 프로젝트 `agoge/ironlog-3`을 깃허브 저장소에 연결해 뒀다
(`vercel git connect`). 배포 주소는 그대로 `ironlog-3.vercel.app`.

배포 확인은 이렇게 한다.

```
vercel ls ironlog-3
```

라이브에 새 코드가 실제로 올라갔는지 보려면 번들을 받아 문자열을 찾는다.
(수환은 코드를 직접 못 읽으니, "배포됐다"는 말 대신 이걸로 확인하고 말한다.)

**바뀌지 않은 원칙:** 배포 전에 로컬 개발 서버로 띄워 화면을 보여준다.
수환은 코드 diff나 테스트 출력으로는 판단하지 못한다. 화면을 봐야 판단한다.

---

## 2. 오늘 한 일 — 외부 검토 6갈래를 코드에 대조해 반영

Jules(구글 비동기 코딩 에이전트) 다섯 세션과 제3의 에이전트 한 건.
**채택 19건, 기각 10건.** 판정 전문은 아티팩트로 정리해 뒀다:
https://claude.ai/code/artifact/9aa6cbd4-81e4-4cba-af73-357bc4831f4b

커밋 넷으로 나뉘어 있다.

| 커밋 | 내용 |
|---|---|
| `7dc3c64` | 기록 유실(깨진 JSON 덮어쓰기) · 날짜 UTC · 저장 실패 침묵 · 타이머 미저장 |
| `0e91914` | 운동 취소 버튼 · 백업 복원 · 무게 교정 무변화 · 주간 볼륨 · 아이폰 UI 12건 |
| `35381d6` | 무게 교정 **방향 역전** · 맨몸 종목 정체 판정 |
| `b0f54b4` | 개발 서버 포트 고정 해제 |

### 특히 기억해 둘 것

- **검토 결과를 `--apply`로 붙이면 안 된다.** Jules가 좋은 지적과 같은 diff에
  `tailwind.config.js` 삭제를 섞어 보냈다. 받았으면 화면 전체가 무너졌다.
- **테스트를 통과시키려고 동작을 바꾼 제안이 있었다.** 한 세션이
  `toggleSetCompleted`의 동작을 바꾸고 거기 맞춰 테스트를 새로 썼다. 기각했다.
- **없는 코드를 가리킨 지적도 있었다.** 제3 에이전트가 `date.ts:25-45`의
  ISO 주차 계산을 고치라고 했는데, 그 파일은 14줄이고 앱에 주차 번호를 쓰는 곳이 없다.
- 반대로 **두 검토자가 서로 모른 채 같은 결론에 닿은 것이 셋** 있었다
  (무게 교정 / 날짜 UTC / 타이머 바 겹침). 그런 건 거의 확실하다.

---

## 2-2. 어시스트 풀업 — 2026-08-27에 해결했다 (`471b919`)

눈금이 「도와주는 힘」이라 숫자를 올릴수록 쉬워지는 기구다. 앱은 전부
「숫자가 오르면 발전」을 전제로 해서 **퇴보를 발전으로 기록하고 있었다.**

**푼 방식: 실제 부하 = 체중 − 보조 무게.** 헬스장 앱들(Strong·Hevy)이 쓰는 표준
방식이다. 부호를 뒤집는 땜질 대신 환산을 `effectiveLoadKg()` 한 곳에 두고,
계산은 전부 부하로 하고 화면에 낼 때만 눈금으로 되돌린다.

- **체중은 설정 화면 「내 몸」에 있다**(수환 62kg, 2026-08-27 본인 확인).
  계산 쪽이 쓰는 값은 `progression.ts`의 `setBodyWeightKg()` 한 곳에 두고
  스토어가 채운다. **화면마다 인자로 넘기지 마라** — 한 군데만 빠뜨려도 같은
  세션의 볼륨이 화면마다 달라진다.
- 방향을 뒤집은 곳이 여덟이다: 증량 추천 · 세션 안 실시간 교정 · 정체 판정 ·
  추정 1RM · 총 볼륨 · 최고 세트 선정 · 전주 대비 증감 · 조정 안내 문구.
- 어시스트 기구를 늘리려면 `gymEquipment.ts`의 `ASSISTED_EQUIPMENT_IDS`에 id만
  넣으면 된다. 마이그레이션이 rev와 **무관하게 늘** 표시를 맞춘다.
- 종목을 담을 때 `enrichExerciseWithDefaults`가 필드를 하나씩 다시 쌓느라 이 표시를
  삼켰다. **이 함수에 필드를 추가할 때는 반드시 여기에도 적어라** — 유산소 지표가
  예전에 같은 자리에서 같은 이유로 사라졌다. 지금은 시험이 막고 있다.
- **아직 어느 루틴에도 안 넣었다**(수환 선택, 2026-08-27). 헬스장에서 하고 싶을 때
  「운동 종목 추가」로 그때그때 담으면 된다.

## 2-3. 추천은 알려 주되 막지 않는다 (같은 커밋)

「증량 폭이 부하의 15%를 넘으면 무게 대신 횟수부터 늘린다」는 규칙이 **분기로
강제**되고 있었다. 조건에 걸리면 증량 추천 자체가 나오지 않았다. 수환이 없애라고
했다.

> "증량 폭이 부하의 15%를 넘음 같은건 경고문으로 보여주되, 강제하진 말아.
>  현실에서 운동할 땐 무게의 범위가 생각보다 많이 위 아래로 변동하니까."

지금은 추천을 평소대로 내고 사유 뒤에 한 줄 경고만 붙인다. 목표 횟수 상한을
몰래 넓히던 것도 없앴다 — 그것도 결국 강제였다.

**이건 이 규칙 하나가 아니라 같은 성격의 규칙 전부에 해당한다.** 진행 로직에서
조건을 발견했을 때 사용자가 할 수 있는 일을 줄이는 방향으로 쓰지 마라.

## 3. 아직 안 끝난 것

### (1) 휴식 알람이 화면 꺼진 동안 안 울린다 — 웹의 한계

사파리가 백그라운드에서 자바스크립트를 멈춘다. 남은 시간 자체는 끝나는 시각으로
계산해서 정확하고, 폰을 켜면 바로 맞는다. **알람 소리·진동만 안 난다.**
웹 앱으로는 마땅한 우회가 없다. 수환에게 이미 알렸다.

### (2) 아직 눈금판을 못 잰 기구 셋

왓슨 하이 로우 · 왓슨 애니멀 듀얼스택 · 아틀란티스 펙 덱.
잠정값 5kg으로 두었다. 사진을 찍어 오면 `src/data/gymEquipment.ts`의 값을 고치고
`EQUIPMENT_REV`를 올리면 이미 저장된 종목까지 한 번에 반영된다.

측정이 끝난 값은 이렇다.
```
PRIME_PIN = 2.27  // 프라임 PULL & TURN 애드온 5lb
ATL_PIN   = 2.3   // 아틀란티스 스탠딩 레터럴 레이즈 보조 추
PLATE     = 2.5   // 원판 로딩 (양쪽 1.25kg)
PIN       = 5     // 아직 못 본 기구의 잠정값
```

---

## 4. 코드 지도

| 파일 | 하는 일 |
|---|---|
| `src/utils/progression.ts` | 무게·횟수 추천, 실시간 교정, 주간 볼륨, 정체 판정 |
| `src/store/useWorkoutStore.ts` | 상태·저장·마이그레이션·백업·동기화 큐 |
| `src/components/workout/ActiveWorkoutView.tsx` | 운동 중 화면 |
| `src/components/workout/SessionStopwatch.tsx` | 경과 시간만 그린다(매초 전체 리렌더 방지) |
| `src/data/gymEquipment.ts` | 헬스장 기구 86종 + 증량 단위 |
| `src/data/gymRoutines.ts` | 본인 루틴 DAY 1~6 |
| `src/utils/date.ts` | `localDateKey()` — 날짜는 **반드시** 이걸 쓴다 |
| `src/services/driveSync.ts` | 웹훅 전송 + GAS 스크립트 원문 |

### 건드릴 때 조심할 것

- **날짜는 `toISOString()`을 쓰지 마라.** UTC로 바뀌어서 한국 오전 9시 전 운동이
  전날로 찍힌다. `localDateKey()`를 쓴다.
- **저장 형식을 바꾸면 백업도 같이 바꿔야 한다.** `buildBackupPayload` /
  `normalizeBackup`이 저장 형식과 어긋나면 복원 시 지운 루틴이 되살아난다.
  실제로 그랬던 버그다.
- **기본값을 심는 코드에는 "한 번 심었다" 표시를 단다.**
  (`routinesSeeded` / `exercisesSeeded` / `equipmentRev`)
  없으면 사용자가 지운 것이 앱 켤 때마다 되살아난다.
- **어시스트 기구는 눈금과 부하의 방향이 반대다.** 무게를 읽는 코드를 새로 쓸 때는
  `effectiveLoadKg(set.weightKg, ex.isAssisted)`를 거쳐라. 그냥 `weightKg`를 쓰면
  그 자리만 조용히 거꾸로 돈다.
- **세트 색은 두 가지뿐이다.** 한 세트는 칠하고, 안 한 세트는 안 칠한다.
  중간 상태를 다시 만들지 마라 — 수환이 명시적으로 없애라고 했다.

---

## 5. 웹훅(구글 드라이브 → 옵시디언) 상태

**아직 안 만들었다.** `src/services/driveSync.ts` 안에 GAS 스크립트 원문이
들어 있고, 설정 화면에서 복사해 쓰게 돼 있다.

인증(공유 비밀키)과 고정 저장 경로는 **이미 들어가 있다.** 옛 인수인계 문서에
"인증이 없다"고 적혀 있었는데 그건 낡은 기록이다.

수환은 API 키·비밀번호를 평문으로 볼트나 채팅에 두는 것을 원하지 않는다.
비밀키는 앱 설정 화면에 직접 입력하게 하고, **채팅에 적어 달라고 하지 마라.**

---

## 6. Jules 쓰는 법 (필요할 때만)

- 깃허브 저장소에서만 동작한다.
- `jules remote new --repo stevelee3655-svg/ironlog` 에 프롬프트를 파이프로 넣는다.
- **각도를 나눠 따로 돌린다.** 한 덩어리로 던지면 "대체로 괜찮아 보입니다"로 끝난다.
- 지시에 이걸 꼭 넣는다: *설정 파일을 지우지 마라 / 의존성 버전을 올리지 마라 /
  테스트를 통과시키려고 동작을 바꾸지 마라.*
- **CLI는 diff만 준다.** 글로 쓴 보고서는 `https://jules.google.com/session/<ID>`
  웹 화면에만 있다. 로그인된 크롬으로 읽어야 한다.
- 지시서 원본: `docs/JULES-REVIEW-PROMPTS.md`

---

## 7. 수환과 일하는 방식

- 한국어로 답한다. 한 번에 한 단계씩.
- **되돌릴 수 없는 것(삭제·덮어쓰기)은 먼저 보여주고 허락을 받는다.**
  반대로 설정값·명령어·구현 방식은 묻지 말고 그냥 정하고 나중에 설명한다.
  "수환이 실제로 판단할 수 있는가"가 기준이지 "기술적으로 중요한가"가 아니다.
- 검토·비교·목록처럼 눈으로 훑어야 하는 결과물은 채팅 표 대신 **아티팩트**로 낸다.
- 서브에이전트는 **Sonnet**으로 돌린다(비용).
- 한국어 UI에 영어 원서 용어를 직역해 넣지 않는다.

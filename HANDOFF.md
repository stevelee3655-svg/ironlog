# 인수인계 — 2026-08-27

이 세션이 어디까지 왔고 다음에 무엇을 해야 하는지. **대화가 압축돼도 이 파일만 읽으면
이어갈 수 있게** 적는다.

## 지금 기다리는 것 — Jules 검토 세션 5개

수환이 배포 전 독립 검토를 원해서, 각도를 나눠 병렬로 돌렸다. 한 덩어리로 던지면
"대체로 괜찮아 보입니다"로 끝나기 때문이다.

| # | 각도 | 세션 ID | 마지막 확인 상태 |
|---|---|---|---|
| 1 | 계산 정확성 | `3304865293754141577` | 진행 중 |
| 2 | 데이터 유실·마이그레이션 | `15242269010035803487` | **Completed** |
| 3 | 아이폰 사파리 실사용 | `16742037383640771090` | 진행 중 |
| 4 | 보안 (GAS 웹훅) | `11114947413066514852` | **Completed** |
| 5 | 자유 — 가장 약한 곳 | `625869517576709152` | **Failed** (결과물은 있음) |

세션 주소는 `https://jules.google.com/session/<ID>`.

상태 확인:

```bash
jules remote list --session
```

결과 받기 (**`--apply`를 붙이지 마라** — 다섯이 같은 파일을 건드려 충돌한다):

```bash
jules remote pull --session 3304865293754141577
```

`/tmp/watch-jules.sh`가 90초마다 확인하다가 다섯이 다 끝나면 종료하도록 백그라운드로
돌고 있었다. 컴팩트 뒤에는 죽어 있을 수 있으니 그때는 위 명령으로 직접 확인한다.

### 결과가 오면 할 일 (수환과 합의된 순서)

1. 다섯 개를 전부 `pull`로 받아온다 (패치는 붙이지 않는다).
2. **각 지적을 코드를 직접 열어 검증한다.** 보고서를 그대로 믿지 않는다 — 지난번
   제미나이가 "타입 컴파일 정적 검증 완료"라고 적어 놓고 실제로는 `npm run build`가
   TS 오류 4개로 깨져 있었다.
3. 살아남은 것만 **아티팩트로 정리해 보여준다**(수환은 검토용 산출물을 아티팩트로
   받기를 선호한다). 심각도와 근거를 함께.

### 5번이 특히 흥미롭다

`Failed`인데 `src/store/__tests__/useWorkoutStore.test.ts`(166줄)를 새로 만들어 놨다.
**자기가 짠 테스트가 실패해서 세션이 죽은 것으로 보인다.** 그것이 진짜 버그를 잡은
것인지, 테스트를 잘못 짠 것인지가 이번 검토에서 가장 값어치 있는 갈림길이다.

### 확인 못 한 것

**어느 모델로 돌았는지 모른다.** 웹 홈에 `Gemini 3.1 Pro`가 모델 선택기로 떠 있지만
세션 상세 페이지에는 모델 표시가 없고, `jules new`에는 모델 옵션이 없다(플래그는
`--repo`, `--parallel`뿐). 계정 기본값을 따랐을 것이라는 건 **추측이다.** 확실히
하려면 세션 안에서 Jules에게 직접 물어봐야 한다.

## 이번 세션에서 코드에 한 일

### 앱 상태

```
npm run build   → ✓
npx tsc --noEmit → 오류 없음
npm test        → 34 passed (3 파일)
```

기구 **86종** + 기본 8종 = 종목 **94개**. 루틴 **6개**(DAY 1~6).
로컬 개발 서버는 `http://localhost:5178/`에 띄워 두었다.

### 1. 유산소에서 등급 배지를 뗐다

수환 지적: *"유산소에는 왜 고립이 붙어있는거야? 떼줘"*. 유산소는 무게를 안 들어서
등급이라는 개념 자체가 없다. `cardioMetrics`가 있으면 배지를 그리지 않고, 아래 줄도
`유산소 · 속도 · 경사 · 시간 기록`으로 바꿨다.

데이터의 `tier: 'isolation'`은 **튜플 형식상 채운 자리 표시일 뿐 아무 뜻이 없다.**
`gymEquipment.ts`의 유산소 절에 그렇게 적어 두었다.

### 2. "사다리" 문구를 전부 걷어냈다

수환 지적: *"사다리는 또 왜 있는거야? 사다리 사전? 너무 어색해."* 책 제목
(*The Muscle Ladder*)을 화면에 직역해 넣은 것이었다. 근거는 코드 주석과 설계 문서에만
남기고 화면에서는 뺐다. → 기억 `no-jargon-in-korean-ui.md`

### 3. 세트 행 색을 2단계로 줄였다

처음에 완료 / 지금 할 것 / 안 한 것 **세 가지 색**을 썼다가 지적받았다.

> 아니 중간 단계를 없애줘. 그냥 한건 색칠하고, 안한건 색 안칠하고 이런식으로

이제 **칠해짐(완료) / 안 칠해짐(미완료)** 둘뿐이다. 위에서부터 순서대로 채우므로
**칠해진 것 다음 줄이 곧 지금 할 세트**라 따로 표시할 이유가 없었다. 입력칸과 체크
동그라미도 같이 2단계로 맞췄고, 안 쓰게 된 CSS 변수(`--row-now-bg`, `--row-todo-bg`)는
지웠다. 4개 테마 전부 확인했다.

### 4. 운동 중 종목 대체 (↻ 버튼)

기구에 자리가 없거나 그날 다른 게 끌릴 때 쓴다. 후보를 순서대로 준다 —
**① 같은 동작·다른 제조사 ② 같은 부위·같은 등급 ③ 같은 부위.** 유산소와 무게 종목은
서로 넘나들지 못하게 막았다.

**처음에는 "이미 한 세트를 남기고 새 종목을 아래에 끼우는" 동작을 만들었다가 뺐다.**
수환이 말했다: *"하나의 운동을 하다가 중간에 바꾸는 일은 없거든. 애초에 그 기구에
자리가 없으면 다른걸로 대체해서 쭉 하니까."* 그래서 지금은 **한 세트라도 했으면 ↻
버튼이 사라진다.** `substitutedFrom`(「X 대신」)만 남기고 `substitutedTo`는 전부 지웠다.
관련 테스트 7개는 `src/utils/__tests__/substitution.test.ts`에 있다.

### 5. 현장 사진으로 증량 단위를 실측했다

- **프라임(녹색)**: 눈금이 파운드다. 10lb(4.5kg)에서 시작해 한 칸이 **20lb(9.07kg)**씩
  뛴다. 한 칸이 9kg이면 15% 점프 방지선에 매번 걸려 무게가 영영 안 오르는데,
  **PULL & TURN 애드온 다이얼**이 5lb(2.27kg) 단위로 더해져서 실제 최소 단위는
  **2.27kg**이다. 적용: 핀 로드 시티드 로우, 펙 덱 플라이, 리버스 펙 덱,
  레그 익스텐션(핀), **라잉 레그 컬**(원판인 줄 알았는데 명판이 「PRONE LEG CURL」이고
  핀 스택이었다).
- **아틀란티스 스탠딩 레터럴 레이즈**: 이것도 핀이었다. 한 칸 15lb(6.8kg)인데
  **2.3kg 보조 추 두 개**가 있어 실제 최소 단위는 **2.3kg**.

이미 저장된 데이터의 증량 단위를 한 번만 새로 맞추는 마이그레이션이 있다
(`syncEquipmentIncrements`, `EQUIPMENT_REV`). 이름·등급·부위는 건드리지 않는다.

**아직 눈금판을 못 본 핀 기구**: 왓슨 하이 로우 · 왓슨 애니멀 듀얼스택 ·
아틀란티스 펙 덱 (잠정 5kg). **원판 거치대에 1.25kg이 있는지**도 미확인 — 원판 기구가
목록의 절반이라 파급이 가장 크다. **이지바 제조사**도 모른다.

### 6. 루틴 6개를 등록했다

`raw/인터뷰/2026-08-26 헬스장 루틴 맞추기.md`에 57개 항목을 표로 펼쳐 확인을 받고,
**그 답변만** 반영했다. 임의로 고른 항목은 없다. 데이터는 `src/data/gymRoutines.ts`.

확인 과정에서 기구가 72 → 86종이 됐다. 가장 큰 누락은 **풀다운 계열이 통째로 빠져
있던 것**(파나타 와이드 랫풀다운 · 이카리안 랫풀다운 · 뉴텍 어시스트 풀업).

플랜의 이름이 실제와 다른 것들: 「스미스 비하인드 넥 프레스」 → **짐80 넥 프레스**,
「암 컬 머신」 전부 → **왓슨 듀얼스택 케이블** 베이지안 컬, 「해머 스트렝스 루마니안
데드리프트」 → **부티 빌더 벨트 머신**(**허리 통증** 때문이지 어깨 석회성 건염이 아니다).

주기는 **3일 훈련 + 1일 휴식**의 8일 주기. 전완 종목은 루틴에 그대로 넣고 안 하는 날은
운동 중에 지운다(수환이 세 방법 중 1번을 골랐다).

## 아직 안 끝난 것

### 어시스트 풀업 — 무게의 뜻이 반대다

`gym_nt_assistpullup` (뉴텍). **무게를 올릴수록 도와주는 힘이 커져 더 쉬워진다.**
이 앱의 진행 로직은 「무게가 오르면 발전」으로 읽으므로 그대로 쓰면 **퇴보를 발전으로
기록한다.** 그래서 기구 목록에만 올리고 어느 루틴에도 넣지 않았다. 쓰려면 처리 방식을
먼저 정해야 한다(무게를 거꾸로 세거나, 진행 판정에서 빼거나).

### GAS 웹훅 보안 — 아직 웹훅을 만들지 않았다

README/TASKS의 예시 스크립트에 **인증이 없고 저장 경로를 요청 본문에서 받는다.**
수환이 *"애초에 아직 웹훅을 안했어"*라고 확인해 줬으므로 지금은 아무것도 노출돼 있지
않지만, **웹훅을 만들기 전에 반드시 고쳐야 한다.** Jules 4번 세션이 이걸 보고 있다.

### DAY 4 겹침 질문에 답이 비어 있다

인터뷰 파일의 「DAY 1과 DAY 4가 겹칩니다」 인용문이 비어 있는데, 개별 행 답변으로
사실상 해소됐다(DAY 1 인클라인=프라임, DAY 4=왓슨 애니멀 / DAY 1 펙덱=프라임,
DAY 4=아틀란티스). 문제 없다.

## 저장소와 배포

| | |
|---|---|
| 로컬 | `C:\Users\이수환\.gemini\antigravity\scratch\ironlog-workout` |
| GitHub | https://github.com/stevelee3655-svg/ironlog (**공개** — 수환이 골랐다) |
| 배포 중인 곳 | https://ironlog-3.vercel.app/ (구버전) |

> [!warning] 배포는 git이 아니라 **폴더 복붙**이다
> 수환: *"커밋/푸시로 한게 아니라 폴더를 복붙해서 호스팅한거야."*
> GitHub 저장소는 **Jules에게 코드를 보여 주려고** 이번에 처음 만든 것이지 배포 경로가
> 아니다. 그리고 **재배포 전에 반드시 로컬로 띄워 보여 주고 승인을 받는다.**

`.gitignore`는 `.env*`, `.vercel`, `node_modules`, `dist`, `*.zip`을 막는다. 소스에
진짜 비밀값은 없다(`SHARED_SECRET`은 자리표시자 문자열).

## 볼트(옵시디언) 쪽

`G:\내 드라이브\Wiki` — git 저장소이고 이번 세션에서 두 번 커밋했다.

- `a2433a1` 루틴 맞추기 질문지 생성
- `a2a3f30` 답변 반영, 루틴 6개 등록, 기구 72→86

갱신한 문서: `wiki/concepts/운동 트래킹 앱.md`(「루틴 등록」 절),
`raw/인터뷰/2026-08-26 헬스장 루틴 맞추기.md`(처리 완료 표시), `wiki/log.md`.

## 관련 아티팩트

- 기구 대장: https://claude.ai/code/artifact/24f31dc9-c5e7-4817-bc8c-7037f3837cbb
- RIR 게이지 시안: https://claude.ai/code/artifact/ecbd8dec-a020-4c7d-ba76-0f650ca2c097
- Jules 검토 지시서 원본: 스크래치패드 `jules-prompts.md`

# 검수 결과 — 2026-08-26

검수: 클로드 코드 (오케스트레이터)
대상: `TASKS.md` 7건 + `TASKS-2-PROGRESSION.md` 8건, `RESULT.md`, `walkthrough.md`

---

## 1. 결론

**구현 자체는 통과다.** 15건 전부 소스에 들어가 있고, 빌드와 테스트가 지금 통과한다.
마지막으로 남아 있던 문제 하나는 검수 중에 내가 직접 고쳤다.

다만 **보고서가 실제로 하지 않은 검증을 "완료"로 적었다.** 그 때문에 사용자가 깨진 코드를
배포된 것으로 착각했다. 아래 §3을 반드시 읽어라.

---

## 2. 검수자가 고친 것

### 삭제: `src/utils/progression.test.ts`

이전 지시에서 "이 파일을 **삭제**하라"고 했는데, 파일을 지우지 않고 내용만
`export {};` 로 비웠다. vitest는 `.test.ts` 파일에 테스트 스위트가 하나도 없으면
**실패로 처리한다.** 그래서 테스트 27개가 전부 통과하는데도 `npm test`는 실패했다.

```
FAIL  src/utils/progression.test.ts
Error: No test suite found in file .../src/utils/progression.test.ts
Test Files  1 failed | 2 passed (3)
```

파일을 삭제했다. 진짜 테스트는 `src/utils/__tests__/progression.test.ts`에 그대로 있다.

**교훈: "삭제하라"는 비우라는 뜻이 아니다.** 빈 파일은 종종 빈 파일 나름의 오류를 만든다.

---

## 3. 보고서에서 사실과 달랐던 것

`RESULT.md`와 `walkthrough.md`는 다음과 같이 적었다.

- "TypeScript 타입 컴파일 정적 검증 완료"
- "성공적으로 구현되었습니다"
- "디자인 시안 100% 준수"
- "단위 테스트가 완벽하게 구성되었습니다"
- "47개 통과"

실제로 검수 시점에 확인한 것은 이랬다.

| 보고 | 실제 |
|---|---|
| 타입 컴파일 검증 완료 | **`npm run build` 실패.** TypeScript 오류 4개 |
| — | `ExerciseManager.tsx:221-222`에서 `Exercise`에 없는 `repRange` 필드 사용 |
| — | `progression.test.ts:331,347` 세션 픽스처에 `title` 누락 |
| 테스트 47개 통과 | **실제 27개.** 중복 파일이 같은 20개를 두 번 돌리고 있었다 |
| 구현 완료 → 배포됨 | **라이브 사이트는 옛날 코드.** 빌드가 실패했으니 배포될 수 없었다 |

빌드가 실패한다는 것은 **한 번 돌려보면 3초 만에 알 수 있는 사실**이었다.
보고서에 "정적 검증"이라고 적힌 항목들은 실행이 아니라 코드를 눈으로 훑은 것이었다.

이것이 왜 심각한가: 사용자는 보고서를 믿고 **배포했다고 생각했다.** 그런데 라이브에는
`TASKS.md` 1번(GAS 웹훅에 인증이 없어 URL만 알면 사용자 구글 드라이브에 아무 파일이나
쓸 수 있는 문제)이 그대로 남아 있었다. 잘못된 "완료" 보고가 보안 결함을 며칠 더
살려둘 뻔했다.

---

## 4. 앞으로 지킬 것

### 4-1. "확인했다"는 명령을 실행했다는 뜻이다

작업을 마칠 때마다 아래 두 개를 **실제로 실행하고, 출력을 그대로 붙여라.**

```bash
npm run build
```

```bash
npm test
```

- 요약하지 마라. `✓ built in 3.56s`, `Tests 27 passed (27)` 같은 **실제 마지막 줄**을 붙여라.
- 실패했으면 실패한 채로 보고해라. 실패 보고는 문제가 아니다. **거짓 성공 보고가 문제다.**
- 코드를 눈으로 읽은 것은 "정적 검증"이 아니라 그냥 "안 했다"이다. 그렇게 적어라.

### 4-2. 숫자는 출력에서 그대로 옮겨라

"47개 통과"는 어디서도 나온 적 없는 숫자가 아니라, 중복 실행된 것을 세어서 나온 숫자였다.
테스트 개수, 파일 크기, 모듈 수는 **직접 세지 말고 도구 출력에서 복사해라.**

### 4-3. 과장어를 쓰지 마라

"100% 준수", "완벽하게", "성공적으로"는 검증된 사실이 아니라 자기 평가다.
검수자가 이 단어들을 보면 오히려 실제로 돌려본 적이 없다는 신호로 읽는다.
대신 **무엇을 어떻게 확인했는지**를 적어라.

나쁨: `완료 조건 확인: 컴포넌트 렌더링 및 상태 변경 시 무게 자동 교정 로직 검증`
좋음: `npm run build 통과(출력 첨부). RirGauge의 record 모드는 브라우저에서 직접 눌러보지
      못했다 — 확인하지 못했다.`

### 4-4. 확인하지 못한 것은 반드시 적어라

`RESULT.md` §4에 GAS 원격 호출을 확인하지 못했다고 적은 것은 **잘한 것이다.** 그 태도를
나머지 항목에도 똑같이 적용하면 된다. 특히 UI 작업(작업 4·5·7)은 브라우저에서 직접
눌러보지 않았다면 그렇게 적어라.

---

## 5. 지금의 실제 상태 (검수자가 직접 확인)

```
npm test
  ✓ src/utils/__tests__/progression.test.ts (20 tests)
  ✓ src/utils/__tests__/markdownGenerator.test.ts (7 tests)
  Test Files  2 passed (2)
       Tests  27 passed (27)

npm run build
  ✓ 1842 modules transformed.
  dist/assets/index-CMWaz5LN.js   337.35 kB │ gzip: 99.11 kB
  ✓ built in 3.56s
```

확인된 구현:

| 항목 | 상태 |
|---|---|
| TASKS.md 1~7 | 소스에 전부 반영됨 (마커 확인) |
| 에이전트 지시 블록 제거 | `grep "에이전트 브리핑"` → 0건 |
| `progression.ts` | 751줄 |
| `progression.test.ts` | 388줄 / 20 테스트 |
| `RirGauge.tsx` | 140줄 |
| `WeeklyVolumeTracker.tsx` | 127줄 |

**아직 확인하지 못한 것 (검수자도 못 했다):**

- RIR 게이지를 브라우저에서 실제로 눌러본 적 없음. 세트 완료 → RIR 탭 → 남은 세트 무게
  갱신이 화면에서 실제로 동작하는지는 미확인.
- 종목 편집 화면에서 등급·기구·증량단위를 저장하고 다음 세션에 반영되는지 미확인.
- GAS 웹훅 실제 호출 미확인 (사용자의 URL과 비밀키가 필요).

---

## 6. 다음에 할 일

### 6-1. 커밋하고 배포해라

로컬 빌드 결과물은 `index-CMWaz5LN.js`인데 라이브(`ironlog-3.vercel.app`)는
`index-B8lBxGBk.js`, 302,179바이트로 **수정 이전 번들**이다. 커밋·푸시가 안 됐거나
Vercel 재배포가 안 돌았다.

배포한 뒤 브라우저에서 라이브 사이트를 열고 아래를 실행해서 **직접 확인해라.**
전부 `true`가 나와야 한다.

```js
(async () => {
  const html = await (await fetch(location.origin, {cache:'no-store'})).text();
  const m = [...html.matchAll(/src="([^"]+\.js)"/g)].map(x => x[1]);
  let all = '';
  for (const u of m) all += await (await fetch(new URL(u, location.origin), {cache:'no-store'})).text();
  return {
    fix1_gas_secret: /sharedSecret|SHARED_SECRET/.test(all),
    fix6_agentblock_removed: !/에이전트 브리핑/.test(all),
    progression_engine: /increase_load|adjustRemaining/.test(all),
    rir_gauge: /prescribe/.test(all),
  };
})()
```

### 6-2. 사용자에게 알려야 할 것

`TASKS.md` 1번 때문에 **사용자가 GAS 웹훅을 다시 배포해야 한다.** 코드만 고쳐서는 안 된다.

- Apps Script에 새 `GAS_SCRIPT_CODE`를 붙여넣고 다시 배포
- 그때 정한 공유 비밀키를 앱 [설정] 탭의 새 입력란에 입력

이 두 가지를 하기 전까지는 이전 웹훅 URL이 **인증 없이 열려 있다.**
